"""LLM integration module for AgroPredict AI.

Provides optional LLM-augmented recommendations and review via a configurable adapter.
When LLM_ENABLED=false (default), all agents use deterministic fallback logic.
"""
